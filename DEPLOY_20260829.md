# 【デプロイの手へ】8/15からの積み残し＋日程変更機能を、1回でまとめて反映する

> このファイルは **「デプロイの手」セッションに渡すための指示書**です。
> 下の「📋 ここからコピー」〜「📋 ここまでコピー」をまるごとコピーして、
> デプロイの手のチャットに貼り付けてください。それだけで伝わります。
>
> **`DEPLOY_20260815_SEATFIX.md` の積み残しは、このファイルが引き継ぎます。**
> 向こうを別途やる必要はありません（同じ作業が入っています）。

---

## まず、これが何の作業か（オーナー向け・1分で読めます）

### 言葉の意味

| 言葉 | 意味 |
|---|---|
| **Worker（ワーカー）** | LINEの予約フォームを動かしている**プログラム本体**。お店でいう「レジの機械」 |
| **D1（ディーワン）** | 開催日やお客様の予約が入っている**データの保管庫**。お店でいう「予約ノート」 |
| **デプロイ** | 直したプログラムを**本番に置きかえる**こと。レジの機械を入れ替えるイメージ |

### やることは3つ。順番どおりに、1回で終わります

| | 内容 | 急ぎ度 |
|---|---|---|
| **①** | 予約ノートに**9月の開催日4件**を足す | 🔴 **8/30のお昼12時まで**。過ぎると予約フォームが空っぽになる |
| **②** | 予約ノートに**「希望日を変更した記録」用の欄**を2つ足す | 通常 |
| **③** | **レジの機械を入れ替える**（席の数え間違いの修正＋日程変更ボタン） | 通常 |

**①と②はデータを入れるだけ。③がプログラムの入れ替えです。**
①→②→③の順でやってください（データの欄を先に作ってから、機械を入れ替えるのが安全なため）。

### ① なぜ8/30のお昼12時が期限なのか

予約フォームは「**その日のお昼12時を過ぎたら、その日の開催日を表示から消す**」作りです。
朝のクラスが終わったあとも出しっぱなしにならないための工夫です。

いま予約ノートに入っている朝クラスは **8/30が最後**です。つまり —

> 🔴 **2026年8月30日のお昼12時を過ぎると、予約フォームの日程がゼロ件になります。**
> そうなると、誰も予約できません。

入れる中身（2026-08-15にトレーナー・シェフから届いた内容。**4回とも確定済み**）:

| 日付 | クラス | 担当 | お食事 |
|---|---|---|---|
| 9/6（日） | はじめてのマシンワークアウト＆巻き肩・背中リセット | GO ／ ふみや | ターメリックライスと三豊ナスのキーマカレー |
| 9/13（日） | ヒップアップトレーニング | GO ／ みどり | わっぱ弁当2種（新米おむすび／さんまの炊き込み） |
| 9/20（日） | お腹まわりを引き締めるウエストシェイプ | 片山めぐみ ／ ふみや | 鶏もも肉のトマトビネガー煮込み |
| 9/27（日） | 呼吸とインナーマッスルのピラティス基礎 | ちひろ ／ みどり | わっぱ弁当2種 |

### ③ で直る・増えるもの

**直るもの（8/15から積み残していた修正）**

「朝RUNのみ」（6:30〜・参加費¥0）の人は7:30のクラスに出ないので、定員10名を使いません。
ところが「この人は数えない」という指示が**4か所のうち1か所にしか入っていませんでした**。
いま本番で起きていること:

- 朝RUNだけの申し込みで、**クラスの残り席が1つ減って見える**
- 朝RUNだけの人が席を埋め、**本当にクラスに出たい人が予約できなくなる**
- 管理画面の人数が `11/10` のような、おかしな数字になる
- 満席の日は、**朝RUNだけの人まで断られる**

4か所とも直してあります。

**増えるもの（8/29の新機能）**

管理画面の体験パーソナル・7日間お試しのリクエストに「📅 希望日を変更」ボタンが増えます。
「第1希望日が過ぎてしまった」「LINEで別日に決まった」ときに、確定の前に日付と時間帯を直せます。

- 直せるのは日付と時間帯だけ。**第2希望・ご要望（お客様が書いた文章）はそのまま残ります**
- 確定済み・不成立済みのリクエストは変更できません
- ⚠️ **お客様への連絡は自動送信されません。** ただしお客様のLINE画面の日付は変わるので、
  **変更したら必ずスタッフからLINEでお伝えください**（画面にも同じ注意を出しています）

---

## オーナーがやること

1. デプロイの手のチャットを開く
2. 下の「📋 ここからコピー」〜「📋 ここまでコピー」をまるごと貼り付ける
3. 終わったと言われたら、**スマホのLINEアプリを開いて**このファイルの一番下の確認をする

---

## 📋 ここからコピー

HACOSのLINE予約システムに、3つの更新を本番へ反映してください。
直したプログラムは GitHub の `goup55/HACOS-` の **main** に入っています（PR #74・#85 マージ済み）。

**①→②→③の順でお願いします。** すべて設定ファイル（`wrangler.toml`）のある
**`apps\worker` の中で**実行してください。

```bash
cd C:\Users\n9-f\.line-harness\apps\worker
```

---

### ① 9月の開催日を入れる（🔴 期限: 8/30のお昼12時まで）

9月の日程が書かれたファイルを取ってきます。

```bash
curl -o sep.sql https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/migrations/2026-09-sessions.sql
```

保管庫に入れます。

```bash
npx wrangler d1 execute line-harness --remote --file=sep.sql
```

ちゃんと入ったか確認します。

```bash
npx wrangler d1 execute line-harness --remote --command "SELECT id, title FROM sessions WHERE date LIKE '2026-09%' ORDER BY date;"
```

**4件（9/6・9/13・9/20・9/27）返ってくれば成功です。件数を報告してください。**

> 💡 間違えて2回実行しても増えたり壊れたりしません（同じ日付は無視される作りです）。
> すでに管理画面から登録済みの日があれば、その日は上書きされずそのまま残ります。

---

### ② 「希望日を変更した記録」の欄を足す

```bash
curl -o resched.sql https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/migrations/2026-08-29-trial-reschedule.sql
```

```bash
npx wrangler d1 execute line-harness --remote --file=resched.sql
```

入ったか確認します。

```bash
npx wrangler d1 execute line-harness --remote --command "SELECT name FROM pragma_table_info('trial_requests') WHERE name IN ('updated_at','updated_by');"
```

**`updated_at` と `updated_by` の2件が返れば成功です。**

> 💡 **`duplicate column name` と出たら、すでに入っています。成功と同じ扱いで先へ進んでください。**
> このファイルは①と違って、2回流すとこのエラーになります（壊れてはいません）。

---

### ③ プログラムを入れ替える

**入れ替えるファイルは2つだけです。** 保管庫（D1）は触りません。お客様の予約データは変わりません。

```bash
curl -o src\reservation-routes.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/reservation-routes.js
```

```bash
curl -o src\admin-page.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/admin-page.js
```

本番に反映します。

```bash
pnpm run deploy
```

`Current Version ID: ...` と出れば成功です。**その番号を報告してください。**

> 💡 予約フォーム（reserve.html）は変わっていないので、**KVの更新は不要**です。

---

### うまくいかないとき（2026-08-15に実際に起きたこと）

**1.「wrangler が見つかりません」と出る**

`wrangler` はパソコン全体ではなく、このプロジェクトの中にしか入っていません。
**`npx wrangler`** のように頭に `npx` を付け、**`apps\worker` の中**で実行してください。

**2.「[code: 7403] The given account is not valid or is not authorized」と出る**

権限がないように見えますが、**たいてい一時的な失敗です。同じコマンドをもう一度打つと通ります。**
2回目でもだめなら報告してください。

**3.「指定されたファイルが見つかりません。」と出る**

`curl -o` を先に実行できているか確認してください。ファイルは `apps\worker` の中に落ちてきます。

---

### もとに戻したいとき

**③を戻す**: 入れ替える前の `src/reservation-routes.js` と `src/admin-page.js` に戻して、
もう一度 `pnpm run deploy`。保管庫は触っていないので、**予約データは無事です。**

**②を戻す**: 戻す必要はありません（欄が増えただけで、何も壊しません）。

**①を戻す**: **まだ誰も9月を予約していない場合だけ**消せます。

```bash
npx wrangler d1 execute line-harness --remote --command "DELETE FROM sessions WHERE date LIKE '2026-09%' AND id = date;"
```

⚠️ **すでに予約が入っていたら消さないでください。** そのときは消さず、受付だけ止めてください。

```bash
npx wrangler d1 execute line-harness --remote --command "UPDATE sessions SET is_open = 0 WHERE date LIKE '2026-09%' AND id = date;"
```

---

### 終わったら報告してほしいこと

- ① 確認コマンドで返ってきた件数（4件になっているか）
- ② `updated_at` / `updated_by` が返ってきたか（または `duplicate column name` が出たか）
- ③ `pnpm run deploy` で出た `Current Version ID` の番号
- 途中でエラーが出たなら、その画面の文字をそのまま

**プログラムやドキュメントの書きかえはしないでください。**
直したほうがいい点に気づいたら、`SESSION_ROLES.md` の連絡板に1行書いて、
オーナー経由で司令塔セッションへ回してください。

## 📋 ここまでコピー

---

## 反映が終わったら：LINEアプリで確認する（オーナー）

> ⚠️ **必ずスマホのLINEアプリの中から開いてください。**
> パソコンのブラウザだと管理者として素通りしてしまい、お客様と同じ画面になりません。

- [ ] 🔴 **9月の日程（9/6・9/13・9/20・9/27）が予約フォームに並ぶ**
- [ ] 🔴 **「朝RUNのみ」で予約しても、残り席が減らない**
      → **予約する前に残り席の数字をメモしてください。** 予約後も同じ数字なら成功です
- [ ] 満席の日があれば、その日でも「朝RUNのみ」なら申し込める
- [ ] 管理画面の人数が `◯/10` の形で、**クラスに出る人だけ**を数えている
      → 朝RUNだけの人は🏃マーク付きで並び、「うち朝RUNのみ ◯名」と出ます
- [ ] 管理画面の体験リクエストに「📅 希望日を変更」が出る。押すとフォームが開き、
      日付を直して保存できる。保存後、受付日時のとなりに「✏️ スタッフが希望日を変更」が出る
- [ ] 変更したあと、**お客様のLINE画面（予約中の表示）も新しい日付になっている**
      → なっていれば正常です。**連絡は自動で飛ばないので、必ずLINEでひとこと伝えてください**

残りのチェック項目は `DEPLOY_20260815_RECORD.md` の「確認の結果」にあります。

---

## このあと残っていること（2026-08-29時点）

- ⚠️ **朝RUNは9月も毎週ある**（8月と同じ・担当GO）という前提で登録しています。
  休む週があれば、その日の「朝RUNあり」のチェックを管理画面で外し、備考の
  「朝RUN 6:30〜あり」も消してください。**外し忘れると、朝RUNのない日に
  「朝RUNのみ（¥0）」で申し込めてしまいます**
- 💡 仕入れの都合でみどりさんの品目が変わったら、管理画面のお弁当欄も直すこと。
  直さないと、お出しできない品をお客様が注文できてしまいます
- 💡 めぐみさんから「丸亀でパーソナルのため8:30には店を出たい」との連絡があります。
  クラスは7:30から45分で8:15に終わるため通常運行のままで収まりますが、
  当日の片づけ・お見送りは他の方でカバーできるようにしておいてください
- 💡 **8/30が終わったら**、LPの「8月の残り」の見出しと2ブロックは削除してよい
  （LPにコメントで書いてあります）
