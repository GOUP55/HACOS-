# 【デプロイの手へ】席の数え方の修正＋9月の日程を入れる

> このファイルは **「デプロイの手」セッションに渡すための指示書**です。
> 下の「📋 ここからコピー」〜「📋 ここまでコピー」をまるごとコピーして、
> デプロイの手のチャットに貼り付けてください。それだけで伝わります。

---

## まず、これが何の作業か（オーナー向け・1分で読めます）

### 言葉の意味

このあと出てくる言葉を先に説明します。

| 言葉 | 意味 |
|---|---|
| **Worker（ワーカー）** | LINEの予約フォームを動かしている**プログラム本体**。お店でいう「レジの機械」 |
| **D1（ディーワン）** | 開催日やお客様の予約が入っている**データの保管庫**。お店でいう「予約ノート」 |
| **デプロイ** | 直したプログラムを**本番に置きかえる**こと。レジの機械を新しいものに入れ替えるイメージ |

### 今回やることは2つ

**A. レジの機械を入れ替える（Workerのデプロイ）**

予約フォームに**数え間違い**が見つかったので、直したものに入れ替えます。

**B. 予約ノートに9月のページを足す（D1にデータを入れる）**

**AとBは、つながっていません。** 片方だけやっても大丈夫ですし、順番も自由です。

### Aで直す「数え間違い」とは

朝RUN（6:30〜）だけ参加して、7:30のクラスには出ない人がいます。
参加費は¥0で、**クラスの席は使いません**。だから、クラスの残り10席を減らしてはいけません。

ところが、その「数えない」という指示が**4か所のうち1か所にしか書かれていませんでした。**

いま本番で起きること:

- 朝RUNだけの申し込みなのに、**クラスの残り席が1つ減って見える**
- 朝RUNだけの人が席を埋めてしまい、**本当にクラスに出たい人が予約できなくなる**
- 管理画面の人数が `11/10` のような、おかしな数字になる

さらに「満席の日でも朝RUNだけなら申し込める」はずが、**満席だと朝RUNだけの人も断られる**状態でした。

4か所とも直してあります。作ったプログラムは全部テスト済み（120項目すべて合格）です。

### Bに期限がある理由

予約フォームは「**その日のお昼12時を過ぎたら、その日の予約を表示から消す**」作りです。
朝のクラスが終わったあとも一日中出しっぱなしにならないための工夫です。

いま予約ノートに入っている朝クラスは **8/30が最後**です。
つまり —

> 🔴 **2026年8月30日のお昼12時を過ぎると、予約フォームの日程が空っぽになります。**
> そうなると、誰も予約できません。

だから、それまでに9月の日程を入れておく必要があります。

### 9月に入れる中身について

2026-08-15 にHMC共有LINEでトレーナー・シェフから届いた内容を入れます。
**まだ全部は決まっていませんが、決まっているところまでで一度入れます**（オーナー決定）。
決まるのを待つと、8/30の期限に間に合わないおそれがあるためです。

| 日付 | クラス | 担当 | お食事 | お弁当の注文 |
|---|---|---|---|---|
| 9/6（日） | **未定** | GO ／ ふみや | ターメリックライスと三豊ナスのキーマカレー | ✅ できる |
| 9/13（日） | **未定** | GO ／ みどり | わっぱ弁当2種（新米おむすび／さんまの炊き込み） | ✅ できる |
| 9/20（日） | お腹まわりを引き締めるウエストシェイプ | 片山めぐみ ／ ふみや | 鶏もも肉のトマトビネガー煮込み（ジャスミンライス付き） | ✅ できる |
| 9/27（日） | 呼吸とインナーマッスルのピラティス基礎 | ちひろ ／ みどり | わっぱ弁当2種（新米おむすび／さんまの炊き込み） | ✅ できる |

**お弁当は4日とも注文できる状態です。**
みどりさん担当の 9/13・9/27 は、**両日とも2品から選べます**（1日に2品を並べるのは今回が初めて）。
お客様は品目ごとのチェックボックスで選び、両方たのむこともできます。
管理画面の集計も「🍱 品名×個数」が品目ごとに分かれて出るので、仕込む数がそのまま読めます。
みどりさんの「仕入れにより変更になる場合はあります」はお客様向けの文言に入れてあります。

まだ決まっていないのは **GOのクラス名（9/6・9/13）だけ**で、
お客様の画面には「朝活クラス（内容は決まり次第お知らせします）」と正直に表示されます。
決まったら**管理画面の「🛠 開催日の管理」で書きかえられます**。この作業をやり直す必要はありません。

---

## オーナーがやること（3ステップ）

1. 「デプロイの手」のチャットを開く
2. 下の「📋 ここからコピー」〜「📋 ここまでコピー」をまるごと貼り付ける
3. 終わったと言われたら、**LINEアプリを開いて**このファイルの一番下の確認をする

---

## 📋 ここからコピー

HACOSのLINE予約システムに、2つの更新を本番へ反映してください。
直したプログラムは GitHub の `goup55/HACOS-` の **main** に入っています（PR #74・マージ済み）。

**AとBは独立した作業です。順番はどちらからでもかまいません。**

---

### 【A】プログラムの入れ替え（数え間違いの修正）

**何が壊れていたか**

「朝RUNのみ」（6:30〜・参加費¥0）の人は7:30のクラスに出ないので、
クラスの定員10名を使いません。ところが「この人は数えない」という指示が
**4か所のうち1か所（予約を受ける前のチェック）にしか入っていませんでした。**
実際に人数を数える側が、全部そのままでした。

| 場所 | 起きていたこと |
|---|---|
| お客様に見える残り席 | 朝RUNだけの申し込みで**残り席が1つ減って見える** |
| 予約を書き込むときのチェック | 朝RUNだけの人が席を埋め、13人ぶんで**本当のクラス予約が入らなくなる** |
| キャンセルした人が予約し直すときのチェック | 同じ |
| 管理画面の人数表示 | クラスに出ない人を数えて `11/10` のような表示になる |

さらに「満席でも朝RUNのみは受け付ける」はずが、書き込む側でひっかかってしまい、
**満席の日は朝RUNだけの人も申し込めない**状態でした。

**入れ替えるファイルは2つだけです。**
データの保管庫（D1）は触りません。お客様の予約データは一切変わりません。

| ファイル | やること |
|---|---|
| `line-reservation/src/reservation-routes.js` | `apps/worker` 側の同じ名前のファイルを置きかえ |
| `line-reservation/src/admin-page.js` | `apps/worker` 側の同じ名前のファイルを置きかえ |

**手順**

設定ファイル（`wrangler.toml`）がある **`apps\worker` の中で**実行してください。
以下はすべて `apps\worker` にいる前提です。

```bash
cd C:\Users\n9-f\.line-harness\apps\worker
```

新しいファイルを取ってきます（そのまま貼れます）。

```bash
curl -o src\reservation-routes.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/reservation-routes.js
```

```bash
curl -o src\admin-page.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/admin-page.js
```

本番に反映します。

```bash
pnpm run deploy
```

`Current Version ID: ...` と出れば成功です。**その番号を報告してください。**

---

### 【B】9月の日程を入れる（期限: 8/30のお昼12時まで）

**なぜ期限があるか**

予約フォームは「日本時間のお昼12時を過ぎたら、その日の開催日を表示から外す」作りです。
保管庫に入っている朝クラスは **8/2・8/9・8/16・8/23・8/30 が最後**なので、
**8/30のお昼12時を過ぎると開催日がゼロ件になり、誰も申し込めなくなります。**

**入れる中身**

9/6・9/13・9/20・9/27 の4日程／定員10名／朝RUNあり。
2026-08-15 にトレーナー・シェフから届いた内容を反映しています。
まだ未定なのは**GOのクラス名2件（9/6・9/13）**だけで、
お客様には「決まり次第お知らせします」と正直に表示されます。
**お弁当は4日とも注文できます。** みどりさん担当の 9/13・9/27 は2品から選べる形です。

> 💡 **もし先に「枠だけ版」を流していても大丈夫です。** このファイルは
> 「入れる」と「上書きする」の両方が入っているので、どちらの状態からでも
> 1回流せば同じ内容に揃います。

**手順**

同じく `apps\worker` の中で実行してください。
**この作業ではプログラムの入れ替え（`pnpm run deploy`）は不要です。** データを入れるだけです。

9月の日程が書かれたファイルを取ってきます。

```bash
curl -o sep.sql https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/migrations/2026-09-sessions.sql
```

保管庫に入れます。

```bash
npx wrangler d1 execute line-harness --remote --file=sep.sql
```

ちゃんと入ったか確認します。

```bash
npx wrangler d1 execute line-harness --remote --command "SELECT id, title FROM sessions WHERE date LIKE '2026-09%' ORDER BY date;"
```

**4件（9/6・9/13・9/20・9/27）返ってくれば成功です。結果を報告してください。**

> 💡 もし間違えて2回実行しても、**増えたり壊れたりしません**（同じ日付は無視される作りです）。
> すでに管理画面から9月を登録済みの日があれば、その日は上書きされず、そのまま残ります。

---

### うまくいかないとき（2026-08-15に実際に起きたこと）

**1.「wrangler が見つかりません」と出る**

`wrangler` はパソコン全体ではなく、このプロジェクトの中にしか入っていません。
**`npx wrangler`** のように、頭に `npx` を付けて呼んでください。
さらに、設定ファイルのある **`apps\worker` の中**にいる必要があります。

**2.「[code: 7403] The given account is not valid or is not authorized」と出る**

権限がないように見えますが、**たいてい一時的な失敗です。**
**同じコマンドをもう一度打つと通ります。** 2回目でもだめなら報告してください。

**3.「指定されたファイルが見つかりません。」と出る**

`curl -o` を先に実行できているか確認してください。
ファイルは `apps\worker` の中に落ちてきます。

---

### もとに戻したいとき

**【A】を戻す**: 入れ替える前の `src/reservation-routes.js` と `src/admin-page.js` に戻して、
もう一度 `pnpm run deploy`。データの保管庫は触っていないので、**予約データは無事です。**

**【B】を戻す**: **まだ誰も9月を予約していない場合だけ**、下で消せます。

```bash
npx wrangler d1 execute line-harness --remote --command "DELETE FROM sessions WHERE date LIKE '2026-09%' AND id = date;"
```

⚠️ **すでに予約が入っていたら消さないでください。** 予約したお客様のデータが迷子になります。
そのときは消さずに、受付だけ止めてください。

```bash
npx wrangler d1 execute line-harness --remote --command "UPDATE sessions SET is_open = 0 WHERE date LIKE '2026-09%' AND id = date;"
```

---

### 終わったら報告してほしいこと

- 【A】`pnpm run deploy` で出た `Current Version ID` の番号
- 【B】確認コマンドで返ってきた件数（4件になっているか）
- 途中でエラーが出たなら、その画面の文字をそのまま

**プログラムやドキュメントの書きかえはしないでください。**
直したほうがいい点に気づいたら、`SESSION_ROLES.md` の連絡板に1行書いて、
オーナー経由で司令塔セッションへ回してください。

## 📋 ここまでコピー

---

## 反映が終わったら：LINEアプリで確認する（オーナー）

> ⚠️ **必ずスマホのLINEアプリの中から開いてください。**
> パソコンのブラウザだと管理者として素通りしてしまい、お客様と同じ画面になりません。

いちばん見たいのはここです。

- [ ] 🔴 **「朝RUNのみ」で予約しても、残り席が減らない**
      → **予約する前に、残り席の数字をメモしてください。**
        予約したあとに同じ数字のままなら成功です。これがAの修正の本体です
- [ ] 満席の日があれば、その日でも「朝RUNのみ」なら申し込める
- [ ] 管理画面の人数が `◯/10` の形で、**クラスに出る人だけ**を数えている
      → 朝RUNだけの人は下に🏃マーク付きで並び、「うち朝RUNのみ ◯名」と出ます
- [ ] 🔴 **9月の日程（9/6・9/13・9/20・9/27）が予約フォームに並ぶ**

残りのチェック項目は `DEPLOY_20260815_RECORD.md` の「確認の結果」にあります。

---

## このあと残っていること

- **まだ決まっていないのは GOのクラス名（9/6・9/13）だけ**
  （決まったら管理画面の「🛠 開催日の管理」から書きかえる）
- 💡 **仕入れの都合でみどりさんの品目が変わったら**、管理画面のお弁当欄も直すこと。
  直さないと、お出しできない品をお客様が注文できてしまいます
- **LPの9月のカードはまだ作っていません。**
  LPのプログラム欄はクラス名・お弁当の説明・先生の名前まで載る作りで、
  いまはGOの2回のクラス名が空いています。埋まってから作ります。
  **9月に入る前に必ず更新してください**（それまでLPは8月のままです）
- 💡 **めぐみさんから「丸亀でパーソナルのため8:30には店を出たい」との連絡があります。**
  クラスは7:30から45分なので8:15に終わり、通常運行のままで収まります。
  当日の片づけ・お見送りだけ他の方でカバーできるようにしておいてください
- ⚠️ **ひとつだけ置いた前提があります: 朝RUNは9月も毎週ある**（8月と同じ・担当GO）
  として登録しました。もし休む週があれば、その日の「朝RUNあり」のチェックを
  管理画面で外し、備考の「朝RUN 6:30〜あり」も消してください。
  **外し忘れると、朝RUNのない日に「朝RUNのみ（¥0）」で申し込めてしまいます**
- ヒーロー下の休講お知らせは 2026-08-17 0:00 に自動で見えなくなりますが、
  部品自体はLPに残っています。9月の更新のときに消してかまいません
