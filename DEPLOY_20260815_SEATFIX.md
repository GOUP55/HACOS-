# 席の数え方の修正＋9月日程の本番反映（LINE Harness側セッション用）

> **使い方**: 下の「📋 貼り付け用プロンプト」を、LINE Harness（`apps/worker`）を
> デプロイする Claude セッション、またはオーナーのPCのターミナルにそのまま貼る。
>
> ✅ **前提はクリア済み**: PR #74 は 2026-08-15 に `main` へマージ済み。
> このファイルの手順はすべて `main` の最新を取りに行く形になっている。
>
> 作業は2つあり、**独立している**（片方だけでも実行できる）。
> - **A. Workerの再デプロイ** … 席の数え方のバグ修正を反映する（2ファイルのみ）
> - **B. 9月の日程をD1へ** … 8/30正午の期限対策。**Workerのデプロイは不要**
>
> ⚠️ **Aを反映するまで、実機確認の「朝RUNのみで残席が減らない」は必ず落ちる。**
> いま本番で動いているのは修正前の版のため。確認はAの後に。

---

## 📋 貼り付け用プロンプト（ここからコピー）

HACOSのLINE予約システムに、2つの更新を本番反映してください。
変更済みソースは GitHub `goup55/HACOS-` の **main** にあります（PR #74・マージ済み）。

### A. 席の数え方のバグ修正（Worker再デプロイ）

**何が壊れていたか**

「朝RUNのみ」（6:30〜・参加費¥0）は7:30の朝活クラスに出ないため、
クラスの定員10名を消費しない設計です。ところが、その除外が
**4箇所のうち1箇所（予約前の事前チェック）にしか入っていませんでした。**
実際に人数を数える側が全部そのままでした。

| 場所 | 起きていたこと |
|---|---|
| お客様に見える残席（`/api/liff/sessions`） | 朝RUNだけの申込みで**クラスの残席が1つ減って見える** |
| 予約を書き込むときの定員ガード | 朝RUNだけの人が席を埋め、13人ぶんで**本当のクラス予約が入らなくなる** |
| キャンセル後に予約し直すときのガード | 同上 |
| 管理画面の人数バッジ | クラスに出ない人を数えて `11/10` のような表示になる |

さらに「満席でも朝RUNのみは受け付ける」という設計が書き込み側で効いておらず、
**満席の日は朝RUNのみも申し込めない**状態でした。4箇所とも修正済みです。

**変えるファイルは2つだけ。マイグレーションもKVの更新も不要です。**

| ファイル | やること |
|---|---|
| `line-reservation/src/reservation-routes.js` | `apps/worker` 側の同ファイルを置換 |
| `line-reservation/src/admin-page.js` | `apps/worker` 側の同ファイルを置換 |

**手順**（`wrangler.toml` のある `apps\worker` の中で実行してください）

```bash
cd C:\Users\n9-f\.line-harness\apps\worker
```

```bash
curl -o src\reservation-routes.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/reservation-routes.js
```

```bash
curl -o src\admin-page.js https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/src/admin-page.js
```

```bash
pnpm run deploy
```

### B. 9月の日程をD1へ入れる（8/30正午の期限）

**なぜ期限があるか**

予約フォームは「JST正午を過ぎたら、その日のセッションを表示から外す」作りです
（当日の朝クラスが終わったあとも一日中出続けるのを防ぐため）。
D1にある朝クラスは **8/2・8/9・8/16・8/23・8/30 が最後**なので、
**2026-08-30 の正午を過ぎると日程がゼロ件になり、誰も申し込めなくなります。**

**入れる内容**（オーナー決定・中身は未定のまま枠だけ先に登録する版）

9/6・9/13・9/20・9/27 の4日程／定員10／朝RUNあり。
クラス名は「朝活クラス（内容は決まり次第お知らせします）」と表示されます。
お弁当・担当は空欄です（フォームでは行ごと出ないので、中途半端な表示にはなりません）。
内容が決まったら**管理画面の「🛠 開催日の管理」から差し替えられます**（SQL不要）。

**手順**（同じく `apps\worker` の中で。**Aとは独立していて、Workerのデプロイは不要**）

```bash
curl -o sep.sql https://raw.githubusercontent.com/GOUP55/HACOS-/main/line-reservation/migrations/2026-09-sessions.sql
```

```bash
npx wrangler d1 execute line-harness --remote --file=sep.sql
```

入ったかの確認:

```bash
npx wrangler d1 execute line-harness --remote --command "SELECT id, title FROM sessions WHERE date LIKE '2026-09%' ORDER BY date;"
```

**4件（9/6・9/13・9/20・9/27）返ってくれば成功です。**
`INSERT OR IGNORE` なので、間違えて2回流しても増えたり壊れたりしません。

### うまくいかないとき（2026-08-15の実績より）

1. **`wrangler` が見つからない**
   PC全体ではなくプロジェクト内にしか無いので、**`npx wrangler`** で呼びます。
   かつ `wrangler.toml` のある **`apps\worker` の中で**実行します。

2. **`[code: 7403] The given account is not valid or is not authorized`**
   権限エラーに見えますが**一時的な失敗**です。同じコマンドを打ち直すと通ります。

3. **`指定されたファイルが見つかりません。`**
   `curl -o` を先に実行し、ファイルが `apps\worker` の中に落ちているか確認してください。

### 切り戻し

**A**: 差し替え前の `src/reservation-routes.js` と `src/admin-page.js` に戻して
`pnpm run deploy`。DBは触らないのでデータは壊れません。

**B**: `DELETE FROM sessions WHERE date LIKE '2026-09%' AND id = date;`
（**まだ誰も予約していない場合のみ**。予約が入っていたら消さず、
`UPDATE sessions SET is_open = 0 WHERE ...` で受付だけ止めてください）

## （ここまでコピー）

---

## 反映のあと：LINEアプリ内で確認する

**必ずLINEアプリの中で確認してください。** PCブラウザは管理者Cookieで素通りするため、
お客様と同じ画面になりません。

チェックリストは `DEPLOY_20260815_RECORD.md` の「確認の結果」にあります。
今回いちばん見たいのはここです。

- [ ] **「朝RUNのみ」で予約しても、その日の残席が減らない**
      （**予約する前に残席の数字をメモしてから**試す。これがAの修正の本体）
- [ ] 満席の日があれば、その日でも「朝RUNのみ」なら申し込める
- [ ] 管理画面の人数バッジが `◯/10` の形で、クラスに出る人数だけを数えている
      （朝RUNのみの方は下に🏃バッジ付きで並び、「うち朝RUNのみ ◯名」と出る）
- [ ] **9月の日程（9/6・9/13・9/20・9/27）がフォームに並ぶ**

## 残っていること（この手順書の範囲外）

- **LPの9月カード**は未着手。LPのプログラム欄はクラス名・お弁当の説明・トレーナー名まで
  載る作りなので、内容が決まってから作ります。**9月に入る前に必ず更新すること**
- **9月のクラス内容**（クラス名・担当・お弁当）が決まったら管理画面から差し替える
- ⚠️ 置いた前提はひとつ: **朝RUNは9月も毎週ある**（8月と同じ・担当GO）として登録しています。
  休む週があれば、その日の「朝RUNあり」のチェックを管理画面で外し、
  備考の「朝RUN 6:30〜あり」も消してください。外し忘れると、朝RUNの無い日に
  「朝RUNのみ（¥0）」で申し込めてしまいます
- ヒーロー下の休講お知らせブロック（`hacos-hmc-lp.html` の `id="noticeHmcRest"`）は
  2026-08-17 0:00 JST で自動非表示になりますが、ブロック自体は残っています。
  9月の月次更新のときに削除してよい
